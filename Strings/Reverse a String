#include <bits/stdc++.h> 
#include <string>
using namespace std; 

int main(){

    string name;
    cout<<"enter the string ";
    getline(cin,name);
    int n = name.length();
    for(int i=n-1;i>=0;i--)
    {
        cout<<name[i];
    }
return 0; 
}
